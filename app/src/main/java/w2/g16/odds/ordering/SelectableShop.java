/*
package w2.g16.odds.ordering;

import w2.g16.odds.model.Shop;

public class SelectableShop extends Shop {

    private boolean isSelected = false;

    public SelectableShop(String shopID, String shopname) {
        super(shopID, shopname);
    }

    public SelectableShop(Shop shop, boolean isSelected) {
        super(shop.getShopID(),shop.getShopname());
        this.isSelected = isSelected;
    }

    public boolean isSelected() {
        return isSelected;
    }

    public void setSelected(boolean selected) {
        isSelected = selected;
    }
}
*/
